{
  "name": "localbase",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @localbase/api --filter @localbase/mcp dev",
    "dev:api": "pnpm --filter @localbase/api dev",
    "dev:mcp": "pnpm --filter @localbase/mcp dev",
    "dev:web": "pnpm --filter @localbase/web dev",
    "mcp": "pnpm --silent --filter @localbase/mcp exec tsx src/index.ts",
    "db:generate": "pnpm --filter @localbase/api db:generate",
    "db:migrate": "pnpm --filter @localbase/api db:migrate",
    "db:studio": "pnpm --filter @localbase/api db:studio",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^20.17.17",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
